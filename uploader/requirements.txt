requests>=2.31.0
ttkbootstrap>=1.10.1
pyperclip>=1.8.2
PyInstaller>=6.0

# Опционально, для более быстрого разбора journal-файлов с большим числом
# строк (например, много ColonisationConstructionDepot рядом со стройкой).
# Не обязателен — при отсутствии код автоматически использует стандартный json.
# orjson>=3.9.0
